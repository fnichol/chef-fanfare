#!/bin/sh -e
exec 2>&1 \
  sudo -H -u <%= @options[:user] %> runsvdir -P /home/<%= @options[:user] %>/service 'log:.........................................................................................................'
